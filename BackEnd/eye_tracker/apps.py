"""App config for eye_tracker."""

from django.apps import AppConfig


class EyeTrackerConfig(AppConfig):
    """Configuration for the eye_tracker app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "eye_tracker"
    verbose_name = "Eye Tracker"
